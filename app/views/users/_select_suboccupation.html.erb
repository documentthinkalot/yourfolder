<% if Suboccupation.where(occupation_id: occupation_id).exists? %>
  <%= collection_select :post, :suboccupation_id, Suboccupation.where(occupation_id: occupation_id), :id, :name, {prompt:"選択してください"}, {class: 'form-control'} %>
<% end %>
