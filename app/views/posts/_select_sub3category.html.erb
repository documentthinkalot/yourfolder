<% if Sub3category.where(sub2category_id: sub2category_id).exists? %>
  <%= collection_select :post, :sub3category_id, Sub3category.where(sub2category_id: sub2category_id), :id, :sub3category_name, {prompt:"選択してください"}, {class: 'form-control'} %>
<% end %>
