<% if Sub5category.where(sub4category_id: sub4category_id).exists? %>
  <%= collection_select :post, :sub5category_id, Sub5category.where(sub4category_id: sub4category_id), :id, :sub5category_name, {prompt:"選択してください"}, {class: 'form-control'} %>
<% end %>
