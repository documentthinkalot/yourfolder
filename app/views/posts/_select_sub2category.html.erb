<% if Sub2category.where(subcategory_id: subcategory_id).exists? %>
  <%= collection_select :post, :sub2category_id, Sub2category.where(subcategory_id: subcategory_id), :id, :sub2category_name, {prompt:"選択してください"}, {class: 'form-control'} %>
<% end %>
